current = current_path + "/" +  params[0] + "/"
l1 = "LEVEL1"
l2 = "LEVEL2"
l3 = "LEVEL3"
get_shell.launch(current + "AutoExploiterRemote", params[1] + " " + params[2] + " " + l3)
get_shell.launch(current + "AutoExploiterRemote", params[1] + " " + params[2] + " " + l2)
get_shell.launch(current + "AutoExploiterRemote", params[1] + " " + params[2] + " " + l1)
